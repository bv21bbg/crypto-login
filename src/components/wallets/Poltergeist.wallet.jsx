import { Tag } from "react-bulma-components";
import { Tool } from "react-feather";

export const PoltergeistWallet = () => {
  return (
    <div>
      <Tag color="warning">
        <Tool />
        In development
      </Tag>
    </div>
  );
};
